/**
 * Shared look of the category filter chips. One constant so the Skills and Projects filters cannot
 * drift apart: they are the same control doing the same job in two sections.
 */
export const FILTER_CHIP_CLASS =
  'h-7 rounded-full px-3 text-xs data-[state=on]:bg-primary data-[state=on]:text-primary-foreground';
