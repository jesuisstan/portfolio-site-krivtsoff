/** Geometry shared by the footer curtain and the stage the last section locks onto. */

/** Width from which the footer leaves the flow and draws across the page. */
export const CURTAIN_QUERY = '(min-width: 768px)';

/** Slice of the panel's top edge held on screen once the curtain is armed — the nav's own `h-16`. */
export const CURTAIN_PEEK_PX = 64;
